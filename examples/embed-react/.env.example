# Dograh API base URL (no trailing slash)
VITE_DOGRAH_API_URL=https://api-production-ccfc.up.railway.app

# Public embed token from Workflow → Settings → Embed (optional here — you can paste in the UI)
VITE_DOGRAH_EMBED_TOKEN=

# In the Dograh dashboard, add this origin to the embed token allowed domains:
# http://localhost:5174
